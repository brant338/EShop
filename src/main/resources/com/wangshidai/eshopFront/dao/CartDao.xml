<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
根标签mapper，一个映射配置文件，就对应一个dao接口
根标签的namespace属性的值就对应dao接口的全限定名
-->
<mapper namespace="com.wangshidai.eshopFront.dao.CartDao">
    <select id="findCartById" parameterType="String" resultType="CartInfo">
        select * from tb_cart where book_id = #{book_id}
    </select>

    <select id="findGoodByCart" parameterType="int" resultType="GoodInfo">
        select * from tb_book
        <foreach collection="list" item="bookId" separator="," open="where book_id in ("
        close=")">
            #{bookId}
        </foreach>
    </select>
</mapper>